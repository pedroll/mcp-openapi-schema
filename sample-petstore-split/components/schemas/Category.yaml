type: object
description: Pet category with a circular reference back to Pet (mascot)
required:
  - name
properties:
  name:
    type: string
    description: Category name
  mascot:
    $ref: './Pet.yaml'
